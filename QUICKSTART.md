# 🚀 Quick Start Guide

## Быстрый старт за 5 минут

### Шаг 1: Замена файлов

1. Скопируйте файлы из этой папки в ваш проект:

```bash
cp templates/index.html /путь/к/вашему/проекту/templates/
cp static/css/style.css /путь/к/вашему/проекту/static/css/
cp static/js/app.js /путь/к/вашему/проекту/static/js/
```

### Шаг 2: Настройка платежей

1. Откройте `payment_integration.py`
2. Замените `YOUR_BOT_TOKEN_HERE` на токен вашего бота
3. Добавьте функции из этого файла в ваш `server.py`

### Шаг 3: Настройка webhook

Запустите в Python консоли:

```python
from payment_integration import setup_webhook

# Замените на ваш домен (обязательно HTTPS!)
setup_webhook("https://your-domain.com/webhook/telegram")
```

### Шаг 4: Проверка

1. Запустите ваш сервер
2. Откройте приложение в Telegram
3. Нажмите "Пополнить" 
4. Выберите сумму
5. Оплатите через Telegram Stars

## ⚠️ Важно!

- **HTTPS обязателен** для webhook
- **Токен бота** не должен быть в клиентском коде
- **Тестируйте** в тестовом режиме Telegram

## 🎨 Кастомизация

### Изменить цвета:

Откройте `static/css/style.css` и найдите:

```css
:root {
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    /* Измените на свои цвета */
}
```

### Изменить суммы пополнения:

Откройте `templates/index.html` и найдите:

```html
<button class="amount-card" onclick="createStarsInvoice(200)">
    <div class="amount-stars">200 ⭐</div>
</button>
```

Добавьте или измените кнопки с нужными суммами.

### Скорость карусели:

В `static/css/style.css` найдите:

```css
.items-carousel {
    animation: scroll-carousel 20s linear infinite;
}
```

Измените `20s` на нужную скорость.

## 🐛 Решение проблем

### Invoice не открывается?

1. Проверьте токен бота в `payment_integration.py`
2. Убедитесь, что webhook настроен: `get_webhook_info()`
3. Проверьте логи сервера

### Карусель не крутится?

Убедитесь, что предметов достаточно (минимум 8) и они дублируются в JavaScript.

### Стили не применились?

Очистите кэш:
- В браузере: Ctrl+Shift+R (Windows) или Cmd+Shift+R (Mac)
- Или добавьте версию: `style.css?v=2`

## 📞 Поддержка

Если что-то не работает:
1. Проверьте console в DevTools браузера (F12)
2. Посмотрите логи сервера
3. Проверьте Network tab для API запросов
4. Убедитесь, что все файлы скопированы правильно

## ✅ Checklist

- [ ] Файлы скопированы
- [ ] Токен бота указан
- [ ] Webhook настроен
- [ ] Сервер запущен на HTTPS
- [ ] Приложение открывается в Telegram
- [ ] Кнопка "Пополнить" работает
- [ ] Invoice открывается
- [ ] После оплаты баланс обновляется

Готово! 🎉
