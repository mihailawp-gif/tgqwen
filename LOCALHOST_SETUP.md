# 🏠 Запуск на localhost - Полная инструкция

## 🎯 Что нужно для локального запуска

Ты можешь запустить всё приложение на своем компьютере БЕЗ домена и сервера!

### Что понадобится:
- ✅ Python 3.10+ 
- ✅ 2 Telegram бота (создадим)
- ✅ ngrok (для HTTPS туннеля) - бесплатно

## 📋 Пошаговая инструкция

### Шаг 1: Установка Python (если нет)

**Windows:**
1. Скачай с [python.org](https://www.python.org/downloads/)
2. При установке ОБЯЗАТЕЛЬНО поставь галочку "Add Python to PATH"
3. Проверь: открой CMD и введи `python --version`

**macOS:**
```bash
brew install python3
```

**Linux:**
```bash
sudo apt update
sudo apt install python3 python3-pip
```

### Шаг 2: Создание Telegram ботов

#### 2.1 Основной бот

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot`
3. Введи название: `My Cases Bot`
4. Введи username: `my_cases_test_bot` (должен быть уникальным)
5. **СОХРАНИ ТОКЕН** - это что-то вроде: `7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw`

#### 2.2 Админ бот (для отправки гифтов)

1. Снова отправь `/newbot` в BotFather
2. Введи название: `My Cases Admin Bot`
3. Введи username: `my_cases_admin_test_bot`
4. **СОХРАНИ ТОКЕН**

#### 2.3 Узнай свой Telegram ID

1. Открой [@userinfobot](https://t.me/userinfobot)
2. Нажми START
3. **СОХРАНИ свой ID** (например: 123456789)

### Шаг 3: Установка ngrok

ngrok создаст HTTPS туннель к твоему localhost.

**Windows:**
1. Скачай с [ngrok.com](https://ngrok.com/download)
2. Распакуй в любую папку (например C:\ngrok)
3. Добавь в PATH или запускай из той папки

**macOS:**
```bash
brew install ngrok
```

**Linux:**
```bash
curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
sudo apt update
sudo apt install ngrok
```

### Шаг 4: Скачай и распакуй проект

```bash
# Распакуй архив
tar -xzf telegram-cases.tar.gz
cd telegram-cases

# Или если уже распаковал, просто зайди в папку
cd telegram-cases
```

### Шаг 5: Установка зависимостей

```bash
# Создай виртуальное окружение (рекомендуется)
python -m venv venv

# Активируй его:
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установи зависимости
pip install -r requirements.txt
```

**Если ошибка с правами на Linux:**
```bash
pip install -r requirements.txt --break-system-packages
```

### Шаг 6: Настройка .env файла

```bash
# Скопируй пример
cp .env.example .env

# Открой в любом редакторе
# Windows: notepad .env
# macOS: open -e .env
# Linux: nano .env
```

**Заполни .env файл:**

```env
# Токен основного бота (из шага 2.1)
BOT_TOKEN=7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw

# Токен админ бота (из шага 2.2)
ADMIN_BOT_TOKEN=7987654321:BBHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw

# ВРЕМЕННО оставь пустым, заполним позже
WEBAPP_URL=

# Твой Telegram ID (из шага 2.3)
ADMIN_IDS=123456789

# Payment token - оставь пустым
PAYMENT_TOKEN=

# База данных - оставь как есть
DATABASE_URL=sqlite+aiosqlite:///./database/cases.db

# Сервер - оставь как есть
HOST=0.0.0.0
PORT=8080
```

**Сохрани файл!**

### Шаг 7: Инициализация базы данных

```bash
# Создай папку для БД
mkdir -p database

# Запусти инициализацию
python database/init_db.py
```

**Должен увидеть:**
```
🔄 Инициализация базы данных...
✅ Таблицы созданы!
🔄 Заполнение примерными данными...
✅ База данных успешно заполнена!
```

### Шаг 8: Запуск ngrok

**Открой НОВЫЙ терминал** (не закрывай текущий!) и запусти:

```bash
ngrok http 8080
```

**Увидишь что-то вроде:**
```
Forwarding    https://abc123.ngrok.io -> http://localhost:8080
```

**ВАЖНО:** 
- Скопируй HTTPS URL (например: `https://abc123.ngrok.io`)
- Это твой временный домен
- НЕ ЗАКРЫВАЙ это окно - ngrok должен работать!

### Шаг 9: Обновление .env с ngrok URL

Вернись к редактированию `.env` и добавь ngrok URL:

```env
WEBAPP_URL=https://abc123.ngrok.io
```

**Сохрани!**

### Шаг 10: Настройка Mini App в BotFather

1. Открой [@BotFather](https://t.me/BotFather)
2. Отправь `/myapps`
3. Отправь `/newapp`
4. Выбери своего основного бота (`My Cases Bot`)
5. Введи название приложения: `Cases`
6. Введи описание: `Open cases and win gifts!`
7. Загрузи фото (можно пропустить - /empty)
8. **ВАЖНО!** На вопрос о Web App URL введи:
   ```
   https://abc123.ngrok.io
   ```
   (твой URL из ngrok)
9. Введи короткое название: `cases`

Готово! Mini App создан!

### Шаг 11: Запуск приложения

**Вариант A: Автоматический запуск (Linux/macOS)**

```bash
chmod +x start.sh
./start.sh
```

**Вариант B: Ручной запуск (Windows или если start.sh не работает)**

Открой **3 ОТДЕЛЬНЫХ терминала** в папке проекта:

**Терминал 1 - Веб-сервер:**
```bash
python server.py
```

**Терминал 2 - Основной бот:**
```bash
python bot/main.py
```

**Терминал 3 - Админ бот:**
```bash
python bot/admin_bot.py
```

**Должен увидеть в каждом терминале сообщения о запуске!**

### Шаг 12: Тестирование! 🎉

1. Открой Telegram
2. Найди своего бота (`My Cases Bot`)
3. Нажми START
4. Должен увидеть приветствие с кнопками
5. Нажми **"🎰 Открыть приложение"**
6. Откроется Mini App!
7. Выбери бесплатный кейс
8. Открой его!
9. Смотри анимацию рулетки
10. Получи приз!

## 🎮 Полный пример для Windows

```bash
# 1. Открой PowerShell или CMD
cd Downloads\telegram-cases

# 2. Создай виртуальное окружение
python -m venv venv
venv\Scripts\activate

# 3. Установи зависимости
pip install -r requirements.txt

# 4. Настрой .env (открой в блокноте)
notepad .env
# Заполни как в шаге 6

# 5. Инициализируй БД
python database\init_db.py

# 6. В НОВОМ окне запусти ngrok
# Открой новый CMD/PowerShell
ngrok http 8080
# Скопируй URL

# 7. Добавь URL в .env
notepad .env
# Добавь WEBAPP_URL=https://твой-ngrok-url

# 8. Настрой Mini App в BotFather (шаг 10)

# 9. Запусти сервисы (в 3 разных окнах):
# Окно 1:
python server.py

# Окно 2:
python bot\main.py

# Окно 3:
python bot\admin_bot.py

# 10. Открой бота в Telegram!
```

## 🐛 Решение проблем

### Ошибка: "No module named 'aiogram'"
```bash
pip install -r requirements.txt
```

### Ошибка: "Permission denied" при установке
**Linux:**
```bash
pip install -r requirements.txt --break-system-packages
```

### ngrok показывает "ERR_NGROK_108"
Бесплатная версия ngrok может требовать регистрацию:
1. Зарегистрируйся на [ngrok.com](https://ngrok.com)
2. Скопируй authtoken
3. Выполни: `ngrok authtoken ВАШ_ТОКЕН`

### Mini App не открывается
1. Проверь что ngrok работает
2. Проверь что WEBAPP_URL в .env правильный
3. Проверь что server.py запущен
4. Проверь в BotFather правильный URL

### Ошибка "Bot token is invalid"
Проверь токен в .env - должен быть без пробелов и кавычек.

### База данных не создается
```bash
# Создай папку вручную
mkdir database
# Попробуй снова
python database/init_db.py
```

### Порт 8080 занят
Измени в .env:
```env
PORT=8081
```
И запусти ngrok с новым портом:
```bash
ngrok http 8081
```

## 📝 Важные заметки

### ⚠️ ngrok перезапускается
Каждый раз когда перезапускаешь ngrok - URL меняется!

**Что делать:**
1. Скопируй новый URL
2. Обнови .env → WEBAPP_URL
3. Обнови в BotFather (/myapps → Edit URL)
4. Перезапусти server.py

**Или** используй платный ngrok с фиксированным URL.

### ✅ Логи и отладка

Смотри что происходит в терминалах:
- server.py - запросы к API
- bot/main.py - команды бота
- bot/admin_bot.py - отправка гифтов

### 🔄 Перезапуск после изменений

Если меняешь код:
1. Останови процесс (Ctrl+C)
2. Запусти заново

## 🎯 Что дальше?

### Кастомизация
1. Отредактируй кейсы в `database/init_db.py`
2. Удали БД: `rm database/cases.db`
3. Создай заново: `python database/init_db.py`
4. Перезапусти

### Добавь изображения
Положи картинки в `static/images/`:
- `free-case.png`
- `starter-case.png`
- и т.д.

Или используй URL в базе данных.

### Измени дизайн
Отредактируй `static/css/style.css`:
```css
:root {
    --primary-color: #YOUR_COLOR;
}
```

## 🚀 Для продакшена

Когда готов к реальному запуску:
1. Арендуй VPS (DigitalOcean, Hetzner)
2. Получи домен
3. Настрой Nginx + SSL
4. Используй PostgreSQL
5. Настрой systemd services

Инструкции в README.md

## ✨ Готово!

Теперь у тебя работает полноценное Mini App прямо на localhost!

**Основные команды:**
```bash
# Запуск всего
./start.sh

# Или вручную в 3 терминалах:
python server.py
python bot/main.py  
python bot/admin_bot.py

# Не забудь ngrok в 4-м терминале:
ngrok http 8080
```

**Проверь:**
- [ ] ngrok запущен и показывает URL
- [ ] .env заполнен правильно
- [ ] БД инициализирована
- [ ] 3 процесса работают
- [ ] Бот отвечает в Telegram
- [ ] Mini App открывается

Удачи! 🎉
