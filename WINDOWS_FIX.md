# 🔧 Исправление ошибок - Windows

## Проблема 1: ModuleNotFoundError: No module named 'database'

**Причина:** Python не может найти модуль из-за путей импорта на Windows.

**Решение:**

Используй альтернативный файл инициализации:

```bash
# Вместо:
python database/init_db.py

# Используй:
python init_database.py
```

Этот файл уже в корне проекта и настроен для Windows!

## Проблема 2: ModuleNotFoundError: No module named 'aiohttp_cors'

**Причина:** Не хватает зависимости в requirements.txt

**Решение:**

```bash
# Переустанови зависимости
pip install -r requirements.txt

# Или установи вручную только недостающее:
pip install aiohttp-cors
```

## ✅ Правильный запуск для Windows

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

Если ошибка с правами:
```bash
pip install -r requirements.txt --user
```

### Шаг 2: Инициализация БД

```bash
# Используй ЭТОТ файл (в корне проекта)
python init_database.py
```

### Шаг 3: Запуск сервера

```bash
python server.py
```

### Шаг 4: Открой браузер

http://localhost:8080/test

## 🚀 Один файл для всего (самое простое)

Используй `run_local.py`:

```bash
# Сначала создай БД
python init_database.py

# Потом запусти всё
python run_local.py
```

## 🪟 Для Windows - самый простой способ

### Вариант 1: Batch файл

Двойной клик на `start.bat` - всё запустится автоматически!

### Вариант 2: PowerShell

```powershell
# Установка
pip install -r requirements.txt

# Инициализация
python init_database.py

# Запуск
python server.py

# Открыть браузер
start http://localhost:8080/test
```

## 🐛 Частые ошибки Windows

### Ошибка: pip не найден

```bash
# Используй python -m pip
python -m pip install -r requirements.txt
```

### Ошибка: Python не найден

1. Установи Python с python.org
2. При установке поставь галочку "Add Python to PATH"
3. Перезапусти CMD

### Ошибка: Permission denied

```bash
# Запусти CMD от имени администратора
# Правый клик → "Запуск от имени администратора"
```

### Ошибка: Encoding проблемы

```bash
# Добавь в начало .env файла:
# -*- coding: utf-8 -*-

# Или используй блокнот с кодировкой UTF-8
```

### Ошибка: Порт 8080 занят

Измени в .env:
```
PORT=8081
```

И открой: http://localhost:8081/test

## 📋 Полная инструкция для Windows

```bash
# 1. Открой PowerShell или CMD в папке проекта
cd C:\Users\твой_юзер\Desktop\telegram-cases

# 2. Создай виртуальное окружение (опционально, но рекомендуется)
python -m venv venv
venv\Scripts\activate

# 3. Установи зависимости
pip install -r requirements.txt

# 4. Создай .env
copy .env.example .env
# Отредактируй .env в блокноте (необязательно для теста)

# 5. Инициализируй БД
python init_database.py

# 6. Запусти сервер
python server.py

# 7. Открой браузер
start http://localhost:8080/test
```

## ✅ Проверка что всё работает

После запуска `python server.py` должен увидеть:

```
============================================================
🚀 Telegram Cases Mini App - Server Started!
============================================================
📡 Local:    http://localhost:8080
🌐 Network:  http://0.0.0.0:8080
🔗 Public:   http://localhost:8080
============================================================
📱 Open your Telegram bot and launch the Mini App
⚙️  Press Ctrl+C to stop
============================================================
```

Если видишь это - всё работает! Открывай http://localhost:8080/test

## 🎯 Готовый скрипт запуска для Windows

Создай файл `quick_start.bat`:

```batch
@echo off
echo Installing dependencies...
pip install -r requirements.txt

echo.
echo Initializing database...
python init_database.py

echo.
echo Starting server...
start http://localhost:8080/test
python server.py

pause
```

Двойной клик - и всё запустится!

## 📞 Всё ещё не работает?

1. Проверь версию Python: `python --version` (должна быть 3.10+)
2. Переустанови зависимости: `pip install -r requirements.txt --force-reinstall`
3. Удали папку `__pycache__` если есть
4. Попробуй запустить от администратора

## 💡 Совет

Используй **Visual Studio Code** для работы с проектом:
1. Установи VS Code
2. Открой папку проекта
3. Встроенный терминал работает отлично
4. Есть подсветка синтаксиса и автодополнение

---

**Теперь всё должно работать на Windows!** 🎉

Запускай:
```bash
python init_database.py
python server.py
```

Открывай: http://localhost:8080/test
