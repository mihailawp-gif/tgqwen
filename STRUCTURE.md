# Структура проекта

```
telegram-cases/
│
├── bot/                          # Telegram боты
│   ├── main.py                   # Основной бот (команды, платежи)
│   └── admin_bot.py              # Бот для отправки гифтов
│
├── database/                     # База данных
│   ├── models.py                 # SQLAlchemy модели
│   ├── init_db.py                # Инициализация БД с примерами
│   └── cases.db                  # SQLite база (создается автоматически)
│
├── static/                       # Статические файлы
│   ├── css/
│   │   └── style.css             # Стили приложения
│   ├── js/
│   │   └── app.js                # Логика Mini App
│   └── images/                   # Изображения (добавьте свои)
│       ├── default-case.png      # Изображение кейса по умолчанию
│       └── default-gift.png      # Изображение гифта по умолчанию
│
├── templates/                    # HTML шаблоны
│   └── index.html                # Главная страница Mini App
│
├── logs/                         # Логи (создается автоматически)
│   ├── web.log
│   ├── bot.log
│   └── admin.log
│
├── server.py                     # Веб-сервер и API
├── requirements.txt              # Python зависимости
├── .env.example                  # Пример конфигурации
├── .env                          # Ваша конфигурация (создайте сами)
├── start.sh                      # Скрипт запуска
└── README.md                     # Документация
```

## Описание компонентов

### Bot (`bot/`)

#### main.py
Основной бот отвечает за:
- Команду `/start` и приветствие
- Показ баланса и статистики
- Пополнение через Telegram Stars (Invoice API)
- Админ-панель для администраторов
- Обработку платежей

#### admin_bot.py
Отдельный бот для отправки гифтов:
- Мониторит заявки на вывод
- Отправляет гифты пользователям
- Обновляет статусы выводов
- Работает в фоновом режиме

### Database (`database/`)

#### models.py
SQLAlchemy ORM модели:
- `User` - пользователи
- `Case` - кейсы
- `Gift` - гифты/призы
- `CaseItem` - связь кейсов и гифтов с вероятностями
- `CaseOpening` - история открытий
- `Withdrawal` - заявки на вывод
- `Payment` - история платежей

#### init_db.py
Скрипт инициализации:
- Создает таблицы
- Добавляет примерные кейсы
- Добавляет гифты
- Настраивает вероятности

### Web App (`static/`, `templates/`)

#### HTML (templates/index.html)
Одностраничное приложение с:
- Главным экраном (кейсы, инвентарь, история)
- Экраном предпросмотра кейса
- Анимацией открытия (рулетка)
- Экраном результата
- Модальными окнами

#### CSS (static/css/style.css)
Стили с:
- Адаптивным дизайном
- Анимациями
- Темной/светлой темой Telegram
- Красивыми градиентами
- Эффектами hover/active

#### JavaScript (static/js/app.js)
Логика приложения:
- Интеграция с Telegram Web App API
- API запросы к серверу
- Управление состоянием
- Анимации
- Обновление UI в реальном времени

### Server (`server.py`)

Веб-сервер на aiohttp:
- Раздача статических файлов
- REST API endpoints
- CORS настройки
- Обработка ошибок
- Middleware

### Конфигурация

#### .env
Переменные окружения:
- Токены ботов
- URL приложения
- ID администраторов
- Настройки БД
- Настройки сервера

## Workflow

### 1. Пользователь открывает Mini App

```
Telegram → Mini App (HTML/JS) → API (/api/user/init) → БД
```

### 2. Загрузка кейсов

```
Mini App → API (/api/cases/list) → БД → JSON → Рендеринг
```

### 3. Открытие кейса

```
User Click → Confirm Modal → API (/api/cases/open) → 
  → Проверка баланса → 
  → Вычисление выигрыша → 
  → Сохранение в БД → 
  → Анимация → 
  → Показ результата
```

### 4. Вывод приза

```
User Click → API (/api/withdraw) → БД (создание Withdrawal) →
  → Admin Bot (фоновая обработка) →
  → Отправка гифта →
  → Обновление статуса
```

### 5. История

```
Polling каждые 5 сек → API (/api/history/recent) → БД → 
  → Последние 50 открытий → 
  → Обновление списка
```

## API Endpoints

### User
- `POST /api/user/init` - Создать/получить пользователя
- `GET /api/user/{id}/free-case-check` - Проверить доступность бесплатного кейса

### Cases
- `GET /api/cases/list` - Список всех активных кейсов
- `GET /api/cases/{id}/items` - Предметы в кейсе с вероятностями
- `POST /api/cases/open` - Открыть кейс (требует case_id, user_id)

### Inventory
- `GET /api/inventory/{telegram_id}` - Все выигранные призы пользователя
- `POST /api/withdraw` - Создать заявку на вывод (требует opening_id)

### History
- `GET /api/history/recent` - Последние 50 выигрышей всех пользователей

## База данных

### User
```sql
id, telegram_id, username, first_name, last_name, 
balance (звезды), free_case_available, last_free_case, created_at
```

### Case
```sql
id, name, description, price (звезды), 
image_url, is_free, is_active, created_at
```

### Gift
```sql
id, name, gift_id (Telegram ID), image_url, 
rarity (common/rare/epic/legendary), value, created_at
```

### CaseItem
```sql
id, case_id, gift_id, drop_chance (0-100)
```

### CaseOpening
```sql
id, user_id, case_id, gift_id, 
is_withdrawn, created_at
```

### Withdrawal
```sql
id, user_id, opening_id, 
status (pending/completed/failed), 
created_at, completed_at
```

### Payment
```sql
id, user_id, amount (звезды), 
status, telegram_payment_id, created_at
```

## Безопасность

### Валидация
- Все входные данные валидируются
- SQL injection защита через ORM
- XSS защита в HTML

### Права доступа
- Проверка владельца перед выводом
- Админ-функции только для ADMIN_IDS
- Проверка баланса перед открытием

### Ограничения
- Бесплатный кейс раз в 24 часа
- Rate limiting на API (можно добавить)
- Проверка duplicate openings

## Расширение

### Добавить новый кейс

```python
# В database/init_db.py
new_case = Case(
    name="💎 Мега кейс",
    description="Описание",
    price=500,
    image_url="URL",
    is_free=False
)
```

### Добавить новый гифт

```python
new_gift = Gift(
    name="Super Gift",
    gift_id="telegram_gift_id",
    rarity="legendary",
    value=1000,
    image_url="URL"
)
```

### Изменить вероятности

```python
CaseItem(
    case_id=1, 
    gift_id=1, 
    drop_chance=50.0  # 50%
)
```

Сумма всех drop_chance для одного кейса должна быть 100.

## Производительность

### Оптимизации
- SQLAlchemy ORM с eager loading
- Async/await для всех операций
- Кеширование статических файлов
- Индексы на часто запрашиваемых полях

### Масштабирование
- PostgreSQL вместо SQLite для продакшена
- Redis для кеширования
- Load balancer для множества инстансов
- CDN для статических файлов

## Деплой

### Локально
```bash
./start.sh
```

### На VPS
```bash
# Systemd services
sudo systemctl start cases-web cases-bot cases-admin
```

### Docker (TODO)
```bash
docker-compose up -d
```

## Мониторинг

### Логи
```bash
tail -f logs/web.log
tail -f logs/bot.log
tail -f logs/admin.log
```

### Метрики
- Количество пользователей
- Открытий кейсов
- Доход
- Конверсия

### Алерты
- Ошибки в логах
- Недоступность сервисов
- Проблемы с БД
